import 'package:flutter/material.dart';

class PromotionsNotificationScreen extends StatelessWidget {
  const PromotionsNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Promotions Notification"),
      ),
    );
  }
}
