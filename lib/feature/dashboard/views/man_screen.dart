import 'package:flutter/material.dart';

class ManScreenView extends StatelessWidget {
  const ManScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("MaN Screen"),
      ),
    );
  }
}
