import 'package:flutter/material.dart';

class WomenScreenView extends StatelessWidget {
  const WomenScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Women"),),
    );
  }
}
